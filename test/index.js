'use strict';

var should = require('chai').should(),
	Falline = require('../lib/index');

describe('#escape', function() {
	it('converts & into &amp;', function() {
		'1'.should.equal('1');
	});
});
